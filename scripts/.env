# Connection strings
PROD_DB_URL=postgres://dbuser:password@64.227.136.125:5432/planner
DEV_DB_URL=postgres://dbuser:password@localhost:5432/planner

# Backup
BACKUP_DIR=./backups/postgres
BACKUP_PREFIX=planner_full

MIGRATIONS_DIR=/opt/app/db/migrations

# Dump / restore options (single string each)
PG_DUMP_OPTS="-Fc -Z9 --no-owner --no-privileges"
PG_RESTORE_OPTS="--clean --if-exists --no-owner --no-privileges --disable-triggers"
